# 変数名・関数名提案ツール 仕様書

ノンプロ研の受講生やプログラミング学習者向けに作成した「変数名・関数名提案ツール」の技術および機能仕様書です。

---

## 1. システム概要

本ツールは、日本語で記述された変数の役割（例：「スプレッドシートの最終行」など）や関数の動作（例：「シート名を取得する」「メールを送信」など）から、Google Apps Script (GAS) や VBA、JavaScript、Python など様々なプログラミング言語で利用できる**キャメルケース（lowerCamelCase）**や**スネークケース（UPPER_SNAKE_CASE）**に沿った、分かりやすく直感的な英語の変数名・関数名を提案するWebアプリケーションです。

### 構成の特徴

- **Google Apps Script（HtmlService）**でホストされるWebアプリケーション。
- 利用者が個別のAPIキー（Gemini API等）を用意することなく、完全に**無料・登録不要**で利用できます。
- Google純正の翻訳エンジン（`LanguageApp.translate`）を利用しつつ、プログラミングで頻出する用語や動詞に対応するための**カスタム辞書エンジン**を搭載しています。

---

## 2. システム構成・アーキテクチャ

ツールは以下のファイルで構成され、GASの `HtmlService` によって1つのWebページとしてクライアントに配信されます。

```mermaid
graph TD
    subgraph クライアントサイド (Index.html)
        UI[ブラウザ UI]
        ModeToggle[モード選択トグル (変数名/関数名)]
        JS_Client[JavaScript / イベント制御]
    end
    subgraph サーバーサイド (Code.gs)
        doGet[doGet関数 / 画面配信]
        suggest[suggestVariableName関数]
        Dict[用語・動詞カスタム辞書 (isVerb)]
        Reorder[動詞+名詞 自動並べ替え]
        Translate[LanguageApp.translate]
    end

    UI -->|1. モード選択 & 入力| JS_Client
    JS_Client -->|google.script.run(query, mode)| suggest
    suggest -->|2. 辞書マッチング (isVerb)| Dict
    suggest -->|3. 動詞+名詞 並べ替え| Reorder
    suggest -->|4. 翻訳リクエスト| Translate
    suggest -->|5. ケース・カテゴリ変換| suggest
    suggest -->|JSONレスポンス| JS_Client
    JS_Client -->|DOM反映 / コピー機能| UI
```

---

## 3. 機能仕様

### 3.1. ハイブリッド提案機能 (v_05 仕様)

画面上部のセグメントトグルにより、**「変数名」** と **「関数名」** の生成モードを瞬時に切り替えて利用できます。

#### 【変数名モード】提案カテゴリ一覧
| 変数タイプ | 命名規則・変換特徴 | 変換コードの例（「アクティブシートのID」） |
| :--- | :--- | :--- |
| **通常変数** | キャメルケース（`lowerCamelCase`）。 | `activeSheetId` |
| **Boolean (真偽値)** | `true`/`false` を取る値。日本語に「ある/持/含」が含まれる場合は `has`、それ以外は `is` プレフィックスを付加。 | `hasActiveSheetId` |
| **配列・リスト** | 配列やリスト形式の値。最後の単語を複数形に変換。 | `activeSheetIds` |
| **定数 (SNAKE)** | プログラム全体で不変の定数。大文字スネークケース（`UPPER_SNAKE_CASE`）。 | `ACTIVE_SHEET_ID` |

#### 【関数名モード】提案カテゴリ一覧
| 関数タイプ | 命名規則・変換特徴 | 変換コードの例（「シート名を取得する」） |
| :--- | :--- | :--- |
| **通常関数 (Verb + Noun)** | 英語の命名慣習に従い、動詞を先頭に配置（`lowerCamelCase`）。 | `getSheetName` |
| **Boolean判定関数** | 判定・検証用関数。`is` / `has` プレフィックスを自動付加。 | `isSheetNameExist` |
| **イベントハンドラ** | イベント処理関数。`on` プレフィックスを自動付加。 | `onGetSheetName` |
| **内部・ヘルパー関数** | モジュール内部関数やプライベート関数。`_` プレフィックスを付加。 | `_getSheetName` |

### 3.2. 動詞＋名詞 自動並べ替えロジック
- 日本語では「シート名を取得」のように **名詞 ➔ 動詞** の順で記述されますが、英語の関数名は **動詞 ＋ 名詞**（`getSheetName`）が標準です。
- `DICTIONARY` 内の動詞用語（`取得: get`, `設定: set`, `送信: send`, `作成: create`, `削除: delete`, `更新: update`, `実行: run`, `検索: search` 等）に `isVerb: true` フラグを設定。
- 関数名モード時、抽出された単語リストから動詞を検出し、動詞を先頭に自動で並べ替えます。

### 3.3. クリップボードコピー機能
- 提案された各変数名・関数名の横にあるコピーボタンをクリックすることで、文字列のみをクリップボードにコピーできます。
- コピー完了時、上部に「Copied!」ツールチップが1.5秒間表示されます。

---

## 4. バージョン履歴

| バージョン | 日付 | 概要・仕様 |
| :--- | :--- | :--- |
| **v_00** | 2026-08-14 | 初回バージョン。日本語の役割からキャメルケースの変数名を提案する基本機能、コピー機能、ダークモード・レスポンシブUIを実装。 |
| **v_01** | 2026-08-14 | 変数タイプ選択機能を追加。左右2カラムレイアウトを導入。 |
| **v_02** | 2026-08-14 | ユーザーが変数タイプを選択することなく、すべての命名タイプを一挙一覧表示する仕様に変更。 |
| **v_03** | 2026-08-15 | **【正式安定版】** 出力順序を固定化し、提案タイプ（推奨/標準的）と理由付きで表示。 (`code_v03.gs`, `Index_v03.html` として凍結) |
| **v_04** | 2026-08-15 | **【他言語汎用化＆UIスリム化】** VBAやPython等でも幅広く使用できるよう宣言プレフィックス（`const`/`let`）を取り外し、4カテゴリのシンプルなUIに改修。`token40` バグを修正のうえ `code_v04.gs`, `Index_v04.html` として凍結。 |
| **v_05** | 2026-08-15 | **【関数名・変数名ハイブリッド拡張】** 画面に「変数名 / 関数名」モード切替スイッチを搭載。辞書動詞の自動検出（`isVerb`）と英語慣習に合わせた「動詞＋名詞」自動並べ替えエンジンを実装。関数名用カテゴリ（通常関数, Boolean判定, イベントハンドラ, 内部関数）を追加。 |
| **v_06** | 2026-08-16 | **【IBM System i CL命名規則 & GAS定番表現追加】** IBM System i (OS/400) の 3文字略称ルール（`clShort`: `RTV`, `CRT`, `DLT` 等）を辞書に組み込み、**「1. GAS定番表現」➔「2. 英語組み合わせ関数名」➔「3. 3桁x3 キャメル記法 (例: rtvActSht)」➔「4. 3桁x3 CL風大文字 (例: RTVACTSHT)」** の指定優先順序で提案出力する機能を追加。 |

