# GAS変数名提案ツール 仕様書

ノンプロ研初心者GAS（Google Apps Script）講座の受講生向けに作成した「GAS変数名提案ツール」の技術および機能仕様書です。

---

## 1. システム概要

本ツールは、日本語で記述された変数の役割（例：「スプレッドシートの最終行」など）から、Google Apps Script / JavaScript の一般的な命名規則である**キャメルケース（lowerCamelCase）**に沿った、分かりやすく直感的な英語の変数名を提案するWebアプリケーションです。

### 構成の特徴

- **Google Apps Script（HtmlService）**でホストされるWebアプリケーション。
- 受講生が個別のAPIキー（Gemini API等）を用意することなく、完全に**無料・登録不要**で利用できます。
- Google純正の翻訳エンジン（`LanguageApp.translate`）を利用しつつ、GAS特有の慣習（例: `ss` や `lastRow`）に対応するための**カスタム辞書エンジン**を搭載しています。

---

## 2. システム構成・アーキテクチャ

ツールは以下の2つのファイルで構成され、GASの `HtmlService` によって1つのWebページとしてクライアントに配信されます。

```mermaid
graph TD
    subgraph クライアントサイド (Index.html)
        UI[ブラウザ UI]
        JS_Client[JavaScript / イベント制御]
    end
    subgraph サーバーサイド (Code.gs)
        doGet[doGet関数 / 画面配信]
        suggest[suggestVariableName関数]
        Dict[GAS用語カスタム辞書]
        Translate[LanguageApp.translate]
    end

    UI -->|入力・クリック| JS_Client
    JS_Client -->|google.script.run| suggest
    suggest -->|1. 辞書マッチング| Dict
    suggest -->|2. 翻訳リクエスト| Translate
    suggest -->|3. キャメルケース化| suggest
    suggest -->|JSONレスポンス| JS_Client
    JS_Client -->|DOM反映 / コピー機能| UI
```

---

## 3. 機能仕様

### 3.1. 変数名提案機能

- **入力方式**: 日本語の自然言語入力（例: 「アクティブなスプレッドシート」「送信するメールの宛先」など）。
- **変数タイプ一括提案 (v_02 変更)**: 変数の用途（通常変数const/let、配列、真偽値、定数）を選択することなく、一回のアクションでこれらすべての命名規則に沿った結果を同時に一覧表示します。
  
| 変数タイプ | 命名規則・変換特徴 | 変換コードの例（「アクティブシートのID」） |
| :--- | :--- | :--- |
| **通常変数 (const)** | 再代入しない通常の変数。キャメルケース（`lowerCamelCase`）。コードプレフィックスは `const`。 | `const activeSheetId` |
| **通常変数 (let)** | 再代入する通常の変数。キャメルケース（`lowerCamelCase`）。コードプレフィックスは `let`。 | `let activeSheetId` |
| **Boolean (真偽値)** | `true`/`false` を取る値。日本語に「ある/持/含」が含まれる場合は `has`、それ以外は `is` プレフィックスをキャメルケースで付加。コードプレフィックスは `let`。 | `let hasActiveSheetId` |
| **配列・リスト** | 配列やリスト形式の値。最後の単語を複数形に変換。コードプレフィックスは `const`。 | `const activeSheetIds` |
| **定数 (SNAKE)** | プログラム全体で不変の定数。大文字スネークケース（`UPPER_SNAKE_CASE`）で変換。コードプレフィックスは `const`。 | `const ACTIVE_SHEET_ID` |

- **出力方式**: 以下の2つのバリエーションの変数名を理由と共に提示。
  
| 提案タイプ | 特徴 | 変換例（「スプレッドシートの最終行」で通常変数の場合） |
| :--- | :--- | :--- |
| **推奨（GAS慣習）** | GASで一般的に使われる、シンプルで直感的な名前です。省略形を優先的に適用。 | `lastRow` |
| **標準的** | 言葉の意味をそのまま正確に英訳し、キャメルケース化した名前。 | `spreadsheetLastRow` |

### 3.2. クリップボードコピー機能

- 提案された各変数名の横にあるコピーボタンをクリックすることで、クリップボードに瞬時にコピーできます（プレフィックス `const` / `let` も含めてコピーされます）。
- コピー完了時、対象のボタン上部に「Copied!」というミニツールチップが1.5秒間表示される視覚的フィードバックを行います。
- `navigator.clipboard` API を優先使用し、非対応の古いブラウザ向けに `document.execCommand('copy')` によるフォールバック処理を実装しています。

### 3.3. UI/UX 仕様

- **デザインシステム**: ノンプログラマーの受講生が親しみやすいよう、ノンプロ研カラーの薄いブルー（`#00b0f0`）と濃いブルー（`#1d72b8`）のグラデーションを基調とした、清潔感のあるカードUIを採用。
- **2カラムレイアウト (v_01 追加)**: PCなどの大画面環境では、画面スクロールを不要にするため、左カラムに入力、右カラムに生成結果を表示する左右2カラム構成としています。初期状態では、右カラムに操作ガイドとなるプレースホルダーを表示します。
- **ダークモード対応**: OSやブラウザのシステム設定（`prefers-color-scheme: dark`）を検知し、自動で目に優しいダークテーマに切り替わります。
- **レスポンシブ対応**: PC大画面（2カラム）からタブレット・スマートフォンでの表示（自動で1カラム縦並びに崩れる）まで崩れない、レスポンシブ設計。
- **ローディングアニメーション**: 翻訳・処理待ちの間、美しい無限回転スピナーが表示されます。

---

## 4. 変換アルゴリズム仕様（Code.gs）

日本語からGASに最適な変数名を生成するため、サーバーサイド（`Code.gs`）にて以下のステップで処理を行います。

```
[入力: "アクティブなシートのID"]
   ↓
1. 全文翻訳 (LanguageApp.translate) ──> "ID of active sheet" ──> [キャメルケース化] ──> 候補3: "idOfActiveSheet"
   ↓
2. 辞書マッチング (長い文字数優先で置換)
   - "アクティブなシート" ──> (en: activeSheet, short: sheet) にマッチ
   - "ID" ──> (en: id, short: id) にマッチ
   置換後文字列: "__TOKEN_A__ の __TOKEN_B__"
   ↓
3. 不要語（助詞）の除去とトークン分割
   - "の" や記号を除去し、単語リスト化: ["__TOKEN_A__", "__TOKEN_B__"]
   ↓
4. 各トークンを辞書値に復元（辞書外の単語は個別に LanguageApp で翻訳）
   - 標準ワード: ["activeSheet", "id"]
   - 短縮（GAS慣習）ワード: ["sheet", "id"]
   ↓
5. キャメルケース化および特例ルールの適用
   - 標準的: activeSheetId ──> 候補2: "activeSheetId"
   - 推奨（GAS慣習）: sheetId ──> 候補1: "sheetId"
```

### 特例ルール

- 「最終行（`lastRow`）」や「最終列（`lastColumn`）」が単語リストの末尾に位置する場合、GASの一般的なコーディング慣習に基づき、親要素（`ss` や `sheet`）を省略して、単に `lastRow` / `lastColumn` のみを推奨名として返却します。

---

## 5. カスタム辞書定義（一部抜粋）

GASで頻出する用語を網羅した辞書（`DICTIONARY`）を内蔵しています。誤置換（例：「アクティブなスプレッドシート」の一部が「スプレッドシート」として先に誤マッチする現象）を防ぐため、**日本語の文字数が長い順**に判定されるよう設計されています。

- **主要な定義例**:
  - `アクティブなスプレッドシート` -> en: `activeSpreadsheet`, short: `ss`
  - `スプレッドシート` -> en: `spreadsheet`, short: `ss`
  - `最終行` -> en: `lastRow`, short: `lastRow`
  - `セル範囲` / `範囲` -> en: `range`, short: `range`
  - `値リスト` -> en: `values`, short: `values`
  - `繰り返し` -> en: `index`, short: `i`
  - `添付ファイル` -> en: `attachment`, short: `attachment`
  - `カレンダー` -> en: `calendar`, short: `cal`
  - `メッセージ` -> en: `message`, short: `msg`
  - `レスポンス` -> en: `response`, short: `res`
  - `リクエスト` -> en: `request`, short: `req`

---

## 6. 制約事項・注意点

1. **Googleの翻訳上限**:
   - 本ツールはサーバーサイドで `LanguageApp.translate` を実行するため、Google Workspace アカウントの種別に応じた「1日あたりの翻訳実行回数（Quota）」の制限を受けます。
   - 一般のGoogleアカウント（無料）の場合、通常1日20,000コール程度が上限となります（個人または小規模な講座での利用には十分な枠です）。
2. **Webアプリのアクセス権限**:
   - GASでWebアプリを公開する際、「アクセスできるユーザー」を `全員` または `全員（匿名ユーザー含む）` に設定することで、受講生はGoogleアカウントのログイン有無にかかわらず利用可能になります。
   - `全員` に設定した場合は、利用時にGoogleアカウントへのログインが求められます。
3. **ブラウザのセキュリティ制限**:
   - クリップボードへのコピー機能（`navigator.clipboard`）は、ブラウザのセキュリティ仕様上、**HTTPS接続（GASの公開URL `https://script.google.com/...` はこれに該当）** またはローカル環境（`localhost` / `file://`）でのみ動作します。安全でないHTTP接続下では機能しません。

---

## 7. バージョン履歴

| バージョン | 日付 | 概要・仕様 |
| :--- | :--- | :--- |
| **v_00** | 2026-08-14 | 初回バージョン。日本語の役割からキャメルケースの変数名を提案する基本機能、コピー機能、ダークモード・レスポンシブUIを実装。 |
| **v_01** | 2026-08-14 | 変数タイプ選択機能（通常変数(const)、通常変数(let)、配列・リスト、Boolean、定数(SNAKE)）を追加。初期状態で「通常変数(const)」を選択し、最も使用頻度の低い「定数(SNAKE)」を最下位に配置。また、画面スクロール不要で結果に到達できる「左右2カラムレイアウト（レスポンシブ）」を導入。 |
| **v_02** | 2026-08-14 | ユーザーが変数タイプを選択することなく、通常変数(const/let)、配列・リスト、Boolean、定数(SNAKE)のすべての結果を一挙に一覧表示するように変更。1回のアクションで全タイプの候補を比較・コピー可能に。 |
| **v_03 (凍結)** | 2026-08-15 | **【正式安定版】** 出力順を通常変数(const) → 通常変数(let) → Boolean(真偽値) → 配列・リスト → 定数(SNAKE)に順序保証付き（配列構造）で確定。出力候補を「推奨（GAS慣習）」と「標準的」の2項目構成に整理し、本バージョンで凍結・完成。 |
