# 変数名提案ツール 仕様書

ノンプロ研の受講生やプログラミング学習者向けに作成した「変数名提案ツール」の技術および機能仕様書です。

---

## 1. システム概要

本ツールは、日本語で記述された変数の役割（例：「スプレッドシートの最終行」など）から、Google Apps Script (GAS) や VBA、JavaScript、Python など様々なプログラミング言語で利用できる**キャメルケース（lowerCamelCase）**や**スネークケース（UPPER_SNAKE_CASE）**に沿った、分かりやすく直感的な英語の変数名を提案するWebアプリケーションです。

### 構成の特徴

- **Google Apps Script（HtmlService）**でホストされるWebアプリケーション。
- 利用者が個別のAPIキー（Gemini API等）を用意することなく、完全に**無料・登録不要**で利用できます。
- Google純正の翻訳エンジン（`LanguageApp.translate`）を利用しつつ、プログラミングで頻出する用語（例: `ss` や `lastRow`）に対応するための**カスタム辞書エンジン**を搭載しています。

---

## 2. システム構成・アーキテクチャ

ツールは以下の2つのファイルで構成され、GASの `HtmlService` によって1つのWebページとしてクライアントに配信されます。

```mermaid
graph TD
    subgraph クライアントサイド (Index.html)
        UI[ブラウザ UI]
        JS_Client[JavaScript / イベント制御]
    end
    subgraph サーバーサイド (Code.gs)
        doGet[doGet関数 / 画面配信]
        suggest[suggestVariableName関数]
        Dict[用語カスタム辞書]
        Translate[LanguageApp.translate]
    end

    UI -->|入力・クリック| JS_Client
    JS_Client -->|google.script.run| suggest
    suggest -->|1. 辞書マッチング| Dict
    suggest -->|2. 翻訳リクエスト| Translate
    suggest -->|3. ケース変換| suggest
    suggest -->|JSONレスポンス| JS_Client
    JS_Client -->|DOM反映 / コピー機能| UI
```

---

## 3. 機能仕様

### 3.1. 変数名提案機能 (v_04 汎用化仕様)

- **入力方式**: 日本語の自然言語入力（例: 「アクティブなスプレッドシート」「送信するメールの宛先」など）。
- **変数タイプ一括提案**: 言語固有の宣言プレフィックス（`const` / `let` など）を含まず、あらゆる言語で直接使用できる「純粋な変数名」を以下の4つのカテゴリで同時に一覧表示します。

| 変数タイプ | 命名規則・変換特徴 | 変換コードの例（「アクティブシートのID」） |
| :--- | :--- | :--- |
| **通常変数** | キャメルケース（`lowerCamelCase`）。 | `activeSheetId` |
| **Boolean (真偽値)** | `true`/`false` を取る値。日本語に「ある/持/含」が含まれる場合は `has`、それ以外は `is` プレフィックスを付加。 | `hasActiveSheetId` |
| **配列・リスト** | 配列やリスト形式の値。最後の単語を複数形に変換。 | `activeSheetIds` |
| **定数 (SNAKE)** | プログラム全体で不変の定数。大文字スネークケース（`UPPER_SNAKE_CASE`）。 | `ACTIVE_SHEET_ID` |

- **出力方式**: 余計なバッジや理由文を廃し、クリップボードにそのままコピーできる直感的な変数名のみを提示します。

### 3.2. クリップボードコピー機能

- 提案された各変数名の横にあるコピーボタンをクリックすることで、変数の文字列のみをクリップボードに瞬時にコピーできます。
- コピー完了時、対象のボタン上部に「Copied!」というミニツールチップが1.5秒間表示される視覚的フィードバックを行います。
- `navigator.clipboard` API を優先使用し、非対応の古いブラウザ向けに `document.execCommand('copy')` によるフォールバック処理を実装しています。

### 3.3. UI/UX 仕様

- **デザインシステム**: ノンプロ研カラーの薄いブルー（`#00b0f0`）と濃いブルー（`#1d72b8`）のグラデーションを基調とした、清潔感のあるカードUIを採用。
- **2カラムレイアウト**: PCなどの大画面環境では、画面スクロールを不要にするため、左カラムに入力、右カラムに生成結果を表示する左右2カラム構成。
- **ダークモード対応**: OSやブラウザのシステム設定（`prefers-color-scheme: dark`）を検知し、自動で目に優しいダークテーマに切り替わります。
- **レスポンシブ対応**: PC大画面からスマートフォン表示まで自動調整。
- **ローディングアニメーション**: 翻訳・処理待ちの間、スピナーが表示されます。

---

## 4. 変換アルゴリズム仕様（Code.gs）

日本語から最適な変数名を生成するため、サーバーサイド（`Code.gs`）にて以下のステップで処理を行います。

```
[入力: "アクティブなシートのID"]
   ↓
1. 全文翻訳 (LanguageApp.translate) ──> "ID of active sheet"
   ↓
2. 辞書マッチング (長い文字数優先で置換)
   - "アクティブなシート" ──> (en: activeSheet, short: sheet) にマッチ
   - "ID" ──> (en: id, short: id) にマッチ
   置換後文字列: "__TOKEN_A__ の __TOKEN_B__"
   ↓
3. 不要語（助詞）の除去とトークン分割
   - "の" や記号を除去し、単語リスト化: ["__TOKEN_A__", "__TOKEN_B__"]
   ↓
4. 各トークンを辞書値に復元（辞書外の単語は個別に LanguageApp で翻訳）
   - 標準ワード: ["activeSheet", "id"]
   - 短縮ワード: ["sheet", "id"]
   ↓
5. キャメルケース化およびタイプ別命名規則の適用
   - 通常変数: activeSheetId / sheetId
   - Boolean: hasActiveSheetId
   - 配列: activeSheetIds
   - 定数: ACTIVE_SHEET_ID
```

---

## 5. カスタム辞書定義（一部抜粋）

プログラミングで頻出する用語を網羅した辞書（`DICTIONARY`）を内蔵しています。誤置換を防ぐため、**日本語の文字数が長い順**に判定されるよう設計されています。

- **主要な定義例**:
  - `アクティブなスプレッドシート` -> en: `activeSpreadsheet`, short: `ss`
  - `スプレッドシート` -> en: `spreadsheet`, short: `ss`
  - `最終行` -> en: `lastRow`, short: `lastRow`
  - `セル範囲` / `範囲` -> en: `range`, short: `range`
  - `値リスト` -> en: `values`, short: `values`
  - `繰り返し` -> en: `index`, short: `i`
  - `添付ファイル` -> en: `attachment`, short: `attachment`
  - `カレンダー` -> en: `calendar`, short: `cal`
  - `メッセージ` -> en: `message`, short: `msg`
  - `レスポンス` -> en: `response`, short: `res`
  - `リクエスト` -> en: `request`, short: `req`

---

## 6. 制約事項・注意点

1. **Googleの翻訳上限**:
   - サーバーサイドで `LanguageApp.translate` を実行するため、1日あたりの翻訳実行上限（Quota）があります。
2. **ブラウザのセキュリティ制限**:
   - クリップボードへのコピー機能（`navigator.clipboard`）は、HTTPS接続またはローカル環境でのみ動作します。

---

## 7. バージョン履歴

| バージョン | 日付 | 概要・仕様 |
| :--- | :--- | :--- |
| **v_00** | 2026-08-14 | 初回バージョン。日本語の役割からキャメルケースの変数名を提案する基本機能、コピー機能、ダークモード・レスポンシブUIを実装。 |
| **v_01** | 2026-08-14 | 変数タイプ選択機能を追加。左右2カラムレイアウトを導入。 |
| **v_02** | 2026-08-14 | ユーザーが変数タイプを選択することなく、すべての命名タイプを一挙一覧表示する仕様に変更。 |
| **v_03** | 2026-08-15 | **【正式安定版】** 出力順序を固定化し、提案タイプ（推奨/標準的）と理由付きで表示。 |
| **v_04** | 2026-08-15 | **【他言語汎用化＆UIスリム化】** VBAやPython等でも幅広く使用できるよう「GAS」表記および宣言プレフィックス（`const`/`let`）を取り外し。カテゴリを「通常変数」「Boolean(真偽値)」「配列・リスト」「定数(SNAKE)」の4種類に再整理。バッジ・理由表示を削って純粋な変数名のみをコピーできるシンプルなUIへ改修。 |
